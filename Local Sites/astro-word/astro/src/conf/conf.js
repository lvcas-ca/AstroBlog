const conf = {
    appwriteUrl: String(process.env.APP_APPWRITE_URL),
    appwriteProjectId: String(process.env.APP_APPWRITE_PROJECT_ID),
    appwriteDatabseId: String(process.env.APP_APPWRITE_DATABASE_ID),
    appwriteCollection: String(process.env.APP_APPWRITE_COLLECTION_ID),
    appwriteBucketId: String(process.env.APP_APPWRITE_BUCKET_ID),
}

export default conf